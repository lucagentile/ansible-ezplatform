Invalid WordPress sites configuration: site names in `ezplatform_sites` must have matching entry in `vault_ezplatform_sites`.

Sites without a matching vault entry:
{% for name in ezplatform_sites.keys() | difference(vault_ezplatform_sites.keys()) %}
* `{{ name }}`
{% endfor %}

Update `group_vars/{{ env }}/vault.yml` to continue.

Docs: https://roots.io/trellis/docs/ezplatform-sites/#passwordssecrets
